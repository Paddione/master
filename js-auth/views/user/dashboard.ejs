<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .dashboard-greeting {
            font-size: 1.2rem;
            margin-bottom: 20px;
        }
        .dashboard-actions .btn {
            margin: 10px 5px; /* More vertical margin */
            min-width: 220px; /* Slightly wider buttons */
            display: block; /* Make buttons take full width of their container if centered */
            margin-left: auto;
            margin-right: auto;
            max-width: 300px; /* Max width for centered block buttons */
        }
        .profile-summary {
            background-color: var(--secondary-color);
            padding: 20px;
            border-radius: var(--border-radius);
            margin-bottom: 25px;
            text-align: center;
        }
        .profile-summary h3 {
            margin-top: 0;
            color: var(--heading-text-color);
            font-size: 1.5rem;
        }
        .profile-summary p {
            color: var(--text-color);
            margin-bottom: 8px;
            font-size: 1rem;
        }
        .dashboard-main-action {
            margin-bottom: 15px;
        }
    </style>
</head>
<body>
<%- include('../partials/header') %>

<div class="container">
    <div class="main-content">
        <h2 class="mb-3 text-center">User Dashboard</h2>

        <%- include('../partials/flash-messages') %>

        <% if (currentUser) { %>
            <div class="profile-summary">
                <h3>Hello, <%= currentUser.username %>!</h3>
                <p>Email: <%= currentUser.email %></p>
                <p>Role: <%= currentUser.role %></p>
            </div>
        <% } %>

        <p class="text-center mb-3">Welcome to your dashboard. Ready for some fun?</p>

        <div class="dashboard-actions text-center mt-3">
            <div class="dashboard-main-action">
                <a href="/play-as-guest" class="btn btn-success btn-lg">Play Quiz Game!</a>
            </div>
            <a href="/hall-of-fame" class="btn btn-warning">View Hall of Fame</a>
            <a href="/user/change-password" class="btn btn-info">Change Password</a>
        </div>

    </div>
</div>

<%- include('../partials/footer') %>
</body>
</html>

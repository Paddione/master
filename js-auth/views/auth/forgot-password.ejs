<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .forgot-password-form-container {
            max-width: 450px;
            margin: 0 auto;
        }
    </style>
</head>
<body>
<%- include('../partials/header') %>

<div class="container">
    <div class="main-content">
        <div class="forgot-password-form-container">
            <h2 class="text-center mb-3">Forgot Your Password?</h2>
            <p class="text-center text-muted-text-color mb-3">
                No problem! Enter your email address below, and if an account exists, we'll send you a link to reset your password.
            </p>

            <%- include('../partials/flash-messages') %>

            <form action="/auth/forgot-password" method="POST">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div class="form-group">
                    <label for="email">Email Address</label>
                    <input type="email" id="email" name="email" class="form-control"
                           value="<%= typeof old_input !== 'undefined' && old_input.email ? old_input.email : '' %>"
                           placeholder="Enter your registered email" required>
                </div>
                <button type="submit" class="btn btn-primary w-100">Send Password Reset Link</button>
            </form>
            <p class="text-center mt-3">
                Remembered your password? <a href="/auth/login">Login</a>
            </p>
        </div>
    </div>
</div>

<%- include('../partials/footer') %>
</body>
</html>
